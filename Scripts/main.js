'use strict';

// main.js
document.addEventListener('DOMContentLoaded', () => {
    initializeDisplay();
    initializeHistory();
    initializeTheme();
    initializeCalculator();
    initializeSounds();
});
